
sum = 0
for i in xrange(0,34):
    f = open(settings.PROCESSED_TRAIN_DIR + "/set_0%i"%i)
    datasets = cPickle.load(f)
    f.close()
    train_set, test_set = datasets
    train_x, train_y = train_set
    test_x, test_y = test_set
    sum = sum + train_x[:,0,0,0] + test_x[:,0,0,0]
 

Using Dropout:
3.59%
1.78%
1.59%

Not Using Dropout:
2.5%
1.7%
1.51%


